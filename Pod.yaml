apiVersion: v1
kind: Pod
metadata:
  name: flaskpod
spec:
  containers:
  - name: flaskapp
    image: cks1130cks/flaskapp:latest
    ports:
    - containerPort: 5000
  imagePullSecrets:
  - name: scret
